import XCTest
@testable import MusicVideoPackage

final class MusicVideoPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MusicVideoPackage().text, "Hello, World!")
    }
}
